{
    "workspaces": [
        "core",
        "rt",
        "signer-ledger"
    ],
    "scripts": {
        "lint": "prettier --check '**/src/**.ts'",
        "fmt": "prettier --write '**/src/**.ts'"
    },
    "devDependencies": {
        "cypress": "^6.6.0",
        "prettier": "^2.2.1"
    }
}
