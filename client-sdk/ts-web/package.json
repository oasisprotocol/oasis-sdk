{
    "workspaces": [
        "core",
        "rt",
        "signer-ledger"
    ],
    "scripts": {
        "lint": "prettier --check '**/src/**.ts'",
        "fmt": "prettier --write '**/src/**.ts'"
    },
    "devDependencies": {
        "prettier": "^2.2.1"
    }
}
